pyserial
pynput
sounddevice
# webrtcvad 仅 VAD 模式需要，PTT 模式无需安装
# Python 3.12 及以下可直接安装：pip install webrtcvad
# Python 3.13+：目前 webrtcvad 暂无预编译包，请使用 PTT 模式（audio.mode: ptt）
# webrtcvad
openai
requests
pyyaml
pyobjc-framework-Quartz; sys_platform == "darwin"

# 智谱 AI（zhipuai provider，参考 transmission_assistant 项目）
# https://open.bigmodel.cn/
zhipuai

# .env 文件支持（可选，未安装时 config.py 内置简易解析作为回退）
python-dotenv

# 讯飞语音听写（xunfei provider）
websocket-client
